.sp__mm-amz
  h4 Want to improve your Skill Rating?
  p We recommend using the:
  p.lead.text-center SteelSeries Stratus XL
    br
    small
      a(ng-href="{{ product.link }}" title="{{ product.title }}") Only {{ product.price }} at {{ product.source }}
  .row
    .col-md-6
      a(ng-href="{{ product.link }}" title="{{ product.title }}" target="_blank")
        img(ng-src="{{ product.images[0] }}")
    .col-md-6
      a(ng-href="{{ product.link }}" title="{{ product.title }}" target="_blank")
        img(ng-src="{{ product.images[product.images.length - 1] }}")
  a.btn.btn-primary.btn-block(ng-href="{{ product.link }}" title="{{ product.title }}" style="margin-top:15px")
    | Shop now at Amazon&nbsp;
    sup <small><i class="fa fa-external-link" aria-hidden="true"></i></small>
